# ESP-IDF build output (generated inside the repo).
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Generated in CI when flattening the layered 4 MB defaults (see ci.yml).
sdkconfig.defaults.*.combined
