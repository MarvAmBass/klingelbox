/* nvs_flash.h - host-test stub. Only the partition name constant is consumed
 * by the linked sources (signal_store.c's stats query and orphan sweep). */
#ifndef DB_HOSTTEST_NVS_FLASH_H
#define DB_HOSTTEST_NVS_FLASH_H

#include "nvs.h"

#define NVS_DEFAULT_PART_NAME "nvs"

#endif
